node_modules
.next
**/dist
.env
.env.local
*.log
*.tsbuildinfo
.data/

# Downloaded S&S vendor product images (regenerated by the catalog sync,
# not source — thousands of files, not meant for version control).
public/vendor/
.vercel
.env*
!.env.example
.cursor/

# CloudShell local state (resource IDs; still should not be committed)
infra/cloudshell/.gwg-rds-state/

# Per-environment CloudShell configs may contain a trusted office IP.
infra/cloudshell/config.*.env

# Scratch files from operational work. A `.tmp-cognito-mfa.json` holding MFA
# material was sitting untracked at the root, one `git add -A` away from being
# committed. Anything named this way is working state, never source.
.tmp-*

.DS_Store
